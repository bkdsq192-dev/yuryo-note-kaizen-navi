# MANUAL_TEST_CHECKLIST

購入者へ配布する前に、通常の Chrome または Edge で順番に確認するためのチェックリストです。

## 開始画面
- [ ] 開始画面が開き、タイトル「有料note改善ナビ」と説明文が日本語で正常表示される
  期待結果: 文字化け、`undefined`、`null`、レイアウト崩れがない
- [ ] `最初から始める` を押せる
  期待結果: STEP1 基本情報へ進む
- [ ] `記入例を読み込む` を押せる
  期待結果: STEP1 基本情報へ進み、記入例の注意書きが表示される
- [ ] 保存済みデータがある状態で `続きから始める` が表示される
  期待結果: 保存していたステップへ直接戻る

## STEP1 から STEP6
- [ ] STEP1 基本情報で公開前 / 公開済みを切り替えられる
  期待結果: 選択状態が見た目でも分かる
- [ ] STEP1 の必須項目を空欄にすると案内が出る
  期待結果: 未入力メッセージが日本語で表示される
- [ ] STEP2 でタイトル案1〜3、採用タイトル、採用理由を入力できる
  期待結果: 入力内容が保持される
- [ ] STEP2 の4確認項目すべてで、自己確認 / 根拠・現状 / 次に直すこと / 優先度を入力できる
  期待結果: 4項目とも保存される
- [ ] STEP3 で無料部分5ブロックの現在内容と改善後内容を入力できる
  期待結果: 各ブロックの内容が保持される
- [ ] STEP4 で購入者に渡すものを複数選択できる
  期待結果: 複数チェックしても崩れない
- [ ] STEP4 で有料部分7ブロックと商品説明を入力できる
  期待結果: 入力内容が保持される
- [ ] STEP5 でX投稿2本、Threads投稿2本、投稿日、投稿時刻、画像名、URL、状態を入力できる
  期待結果: 4投稿すべて保存される
- [ ] STEP6 で公開予定日、1日に使える時間、直したい分野、7日分の計画を入力できる
  期待結果: 日付、作業内容、完了条件、状態が保持される

## 20項目の回答と優先3項目
- [ ] 20項目すべてに回答した状態で結果まとめへ進める
  期待結果: `回答済み 20 / 20件` が表示される
- [ ] `明確に書かれている` `一部書かれている` `まだ書かれていない` が混在する入力で集計が変わる
  期待結果: 件数が入力内容どおりに変わる
- [ ] 最優先が3件のとき候補が3件になる
  期待結果: 優先3項目の候補が3件だけ表示される
- [ ] 最優先が4件以上のとき3件に絞れる
  期待結果: 候補は複数出るが、選択は3件まで
- [ ] 最優先が2件以下のとき `次に直す` から補える
  期待結果: 合計3件にできる
- [ ] 選んだ3件の `分野` `根拠・現状` `次に直すこと` が表示される
  期待結果: 選択内容と表示内容が一致する

## 結果まとめと最終設計書
- [ ] 分野別のバー表示が欠けずに表示される
  期待結果: 各分野に `明確 / 一部 / 未整理` の件数が表示される
- [ ] 点数や販売予測が表示されない
  期待結果: スコア表示や販売予測文言がない
- [ ] 最終設計書に採用タイトル、読者、悩み、到達点、無料部分5ブロック、有料部分7ブロック、渡すもの、X投稿2本、Threads投稿2本、優先3項目、7日間改善計画が入る
  期待結果: 必要項目が欠けず、`undefined` `null` `[object Object]` が出ない
- [ ] 空欄の項目がある場合でも表示が崩れない
  期待結果: 空欄は `未記入` などの自然な表示になる

## コピー・印刷・JSON
- [ ] `全文をコピー` が動く
  期待結果: コピーした文章が日本語の見出し付きで読みやすい
- [ ] `印刷する` が開く
  期待結果: 不要な操作ボタンが印刷表示に出すぎない
- [ ] `JSONを書き出す` が動く
  期待結果: `.json` ファイルを保存できる
- [ ] `JSONを読み込む` で正常なバックアップを復元できる
  期待結果: 保存していたステップと内容が戻る
- [ ] 不正なJSONを読み込むとエラーが出る
  期待結果: 日本語エラーメッセージが表示され、アプリが落ちない

## 自動保存と再開
- [ ] 入力後に少し待つと自動保存される
  期待結果: ページ再読み込み後に内容が残る
- [ ] 再読み込み後、保存していたステップから再開する
  期待結果: 開始画面に戻らず、直前のステップが開く
- [ ] `開始画面へ戻る` の後に `続きから始める` が使える
  期待結果: 直前の入力内容へ戻れる
- [ ] `最初からやり直す` の確認ダイアログが出る
  期待結果: キャンセルなら保持、OKなら初期化される

## スマートフォン幅
- [ ] 幅 375px 前後で横スクロールが出ない
  期待結果: 画面全体を横にずらさず使える
- [ ] 長い日本語が画面外へはみ出さない
  期待結果: 見出し、説明、設計書テキストが折り返される
- [ ] 入力欄が画面幅を超えない
  期待結果: テキストエリアやセレクトが右にはみ出さない
- [ ] ボタンが押しやすい
  期待結果: 戻る / 次へ / コピー / JSON操作が指で押しやすい
- [ ] ステップ一覧が操作できる
  期待結果: 各STEPへ移動できる

## 日本語表示とアクセシビリティ
- [ ] ラベルと入力欄の対応が分かりやすい
  期待結果: ラベルクリックでも入力しやすい
- [ ] フォーカス表示が見える
  期待結果: キーボード操作時に現在位置が分かる
- [ ] 色だけで状態を見分けさせていない
  期待結果: テキストでも状態が分かる
- [ ] 全画面で日本語の文字化けがない
  期待結果: READMEやUI文言と同様、自然な日本語で表示される
